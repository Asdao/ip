interface:
  display_name: "Test UI"
  short_description: "Run scripted console UI checks"
  default_prompt: "Use $test-ui to run the project's console test plan and report the exact input and output."
